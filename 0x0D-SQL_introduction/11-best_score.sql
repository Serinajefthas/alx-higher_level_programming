-- lists all records with score >= 10 in second_table
-- displays only score and name rows (in order)
-- ordered by highest score
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
